services:
  - type: web
    name: rls-simulation
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --chdir backend app:app"
    rootDir: "backend"
